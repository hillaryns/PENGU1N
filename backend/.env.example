PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017
DB_NAME=pengu1n
COLLECTION_NAME=userDetails

# Email (Nodemailer) — OTP & transactional mail
# Gmail: use an App Password, not your normal login password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="PENGU1N <noreply@yourdomain.com>"
